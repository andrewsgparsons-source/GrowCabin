# Briefings

Use the Briefings page for a curated, skimmable list:
- `/briefings.html`

Source files:
- `docs/briefings/` (each briefing as its own file)
- `docs/briefings/index.json` (titles, keywords, colors, timestamps)

